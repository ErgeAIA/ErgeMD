# 2026-08-18

## G2.1 前端资产规范审查与优化（ErgeMD）
- 审查来源：AIVault `docs/design-system/tokens.md` G2.1 章节。
- 已执行：
  - P2 清理孤儿资产：命令删除 `public/tauri.svg`（Vite 脚手架残留）、`public/images/app-icon.svg`（logo 唯一来源已内联在 `AppIcon.tsx`）。
  - P3 修瑕疵：`src/styles/markdown.css` 代码字体此前外层多包一层引号，已去除（并入变量替换）。
  - P1 方案 B 落地：在 `src/styles/globals.css` `:root` 定义 `--font-mono: ui-monospace, SFMono-Regular, "SF Mono", Menlo, Consolas, "Liberation Mono", monospace`；`markdown.css` 4 处 + 5 个 tsx 组件（CodeBlock / WelcomePage / MarkdownBlockView / LazyPlantUMLBlock / LazyMermaidBlock）内联代码字体统一改为 `var(--font-mono)`。零打包、跨平台一致。
- 有意保留：`ReadingOptions.tsx:65` 字体选择器中的 `'JetBrains Mono', ...` 是用户可选字体项，非渲染默认值。
- 结论：ErgeMD 未踩 G2.1 硬红线（无 PNG 转 SVG、无硬编码 CJK 字体、图标正确用 currentColor）。
