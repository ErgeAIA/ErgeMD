# 会话压缩快照 20260509-01

> 压缩时间：2026-05-09
> 原会话轮次：约 15 轮
> 层级声明：本快照是**本会话**的唯一事实来源；项目级长期记忆仍以 `./AGENTS.md` 及其引用文件为准。两者冲突时，按"项目级 > 会话级"裁决。

## 1. 已锁定结论

- **类型：修复方案**
  - 结论：Packet diagram 的 themeVariables 存在 [Mermaid 官方 Bug](https://github.com/mermaid-js/mermaid/issues/7058)，themeVariables 无法传播到样式函数
  - 来源：第 15A-R3 用户确认

- **类型：变量名**
  - 结论：Radar 图表颜色应使用 `cScale0`~`cScale4`，而非 `radarCurve0`~`radarCurve4`
  - 来源：第 15A-R3 用户确认

- **类型：方案决策**
  - 结论：Packet / Treemap 改用 CSS 后处理覆盖（因为 themeVariables 不可用）
  - 结论：Radar 修正变量名为 `cScale0`~`cScale4`（曲线颜色），背景/轴标签等仍需 CSS 覆盖
  - 来源：第 15A-R3 用户确认

## 2. 已交付产物清单

| 产物名 | 形式 | 交付轮次 | 状态 |
| ------ | ---- | -------- | ---- |
| Phase 13.7 CSS 变量清理 | 代码修改 | 13.7 | 用户已验收 |
| Phase 13.7-V 运行时验收 | 验收报告 | 13.7-V | 通过 |
| Phase 15A-R2 补链 | MermaidDiagram.tsx 修改 | 15A-R2 | 已交付 |
| Phase 15A-R3 分析报告 | 分析文档 | 15A-R3 | 已交付 |

## 3. 已否决方案

- **方案**：继续使用 `packetTextColor`、`radarCurve0` 等变量名尝试 themeVariables 方式
  - **否决理由**：Packet themeVariables 存在 Mermaid Bug；Radar 实际变量名为 `cScale0`~`cScale4`
  - **否决轮次**：第 15A-R3

## 4. 未决事项

### 4.1 待用户裁决
- **议题**：是否现在执行修正 Radar 变量名（`cScale0`~`cScale4`）和 Packet/Treemap CSS 覆盖方案
  - **情境差异**：A=现在修正，B=后续迭代修正

### 4.2 待验证
- **事项**：Packet / Treemap / Radar 在 Tauri 中的实际渲染效果
- **验证方法**：打开 `docs/MermaidRuntimeIsolation.md`，F12 开发者工具，检查 SVG 元素 fill/stroke 属性

### 4.3 待办
- **任务**：修正 MermaidDiagram.tsx 中 Radar 的 themeVariables 变量名（`cScale0`~`cScale4`）
- **任务**：为 Packet / Treemap 在 mermaid 适配器中实现 CSS 后处理覆盖
- **任务**：更新 AGENTS.md 同步 Phase 13.7 / 13.7-V / 15A-R2 / 15A-R3 完成状态

## 5. 关键字面锚点

- **文件路径**：`src/components/reader/MermaidDiagram.tsx`
- **CSS 路径**：`src/styles/themes/core/mermaid/*.css`
- **变量前缀**：`--mermaid-*`（由适配器映射），`--chart-*`（palette）
- **函数名**：`getMermaidColors()`，`normalizeChartType()`，`getMermaidSourceForTypeDetection()`
- **接口名**：`ChartColors`（包含 flowchart, mindmap, gitgraph, packet, radar, treemap, block, kanban, quadrant, treeview）
- **Mermaid Bug**：Packet themeVariables 不生效（[Issue #7058](https://github.com/mermaid-js/mermaid/issues/7058)）
- **Radar 正确变量名**：`cScale0`~`cScale4`

## 6. 压缩中发现的盲区

- **盲区**：mermaid-packet.css / mermaid-radar.css / mermaid-treemap.css 中的 CSS 选择器可能不准确
- **原因**：未在浏览器中实际检查 SVG 输出结构的 class 名称
- **补充依据**：需要打开 Tauri DevTools Elements 面板检查 `.mermaid svg` 下的实际 DOM 结构

## 7. 建议回写到项目级长期记忆的条目

> 以下条目具备项目级稳定性。建议用户在合适时机执行「同步项目进度快照」指令，将其追加到 AGENTS.md。

- [ ] **类型：踩坑** — 内容：Packet diagram themeVariables 存在 Mermaid Bug（Issue #7058），需用 CSS 覆盖替代
- [ ] **类型：变量名** — 内容：Radar 图表曲线颜色正确变量名为 `cScale0`~`cScale4`，非 `radarCurve0`~`radarCurve4`
- [ ] **类型：约束** — 内容：Treemap（beta 新图表）无 themeVariables，只能通过 classDef 和 CSS 覆盖
- [ ] **类型：决策** — 内容：Packet / Treemap 推荐使用 CSS 后处理覆盖样式，而非 themeVariables

---

*本会话包含 Phase 13.7、13.7-V、15A-R2、15A-R3 的完整交付与验收。*
