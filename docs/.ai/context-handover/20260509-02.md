# 项目上下文交接 - 20260509-02

## 1. 交接元数据

- 生成时间：2026-05-09T22:00:00+08:00
- 关联文档：`./AGENTS.md`（新会话必须先读此文件）
- 当前阶段：Phase 18-R2（阅读交互丝滑度小修）— 代码修改 + TypeScript 检查通过，待 Tauri 人工验证
- 当前目标：等待用户在 Tauri 中验证 Phase 18-R2 四项修复（TOC 柔化、图表编辑按钮、右键编辑段落、Wardley 主题适配）
- 下一窗口首要目标：确认 Phase 18-R2 Tauri 验证通过后，归档 Phase 18 并进入下一阶段

## 2. AGENTS.md 增量摘要

| 小节 | 新增条目摘要 |
| ---- | ------------ |
| 近期工作 | 新增 Phase 17A-Final、Phase 17B-Final、Phase 18-Audit、Phase 18-R1、Phase 18-R2 完成记录 |
| TODOs | 新增 [x] Phase 17A-Final、[x] Phase 17B-R1 已转为 [x] Phase 17B-Final、[x] Phase 18-Audit、[x] Phase 18-R1 完成标记 |
| Decision Log | 新增两条：Phase 17B 设计原则决策、Phase 18-Audit 根因判断 |
| 历史事件与硬性约束 | 新增 Phase 17B 和 Phase 18-R1 事件记录 |

## 3. 本次会话进度纪要

### 已完成的具体任务

**Phase 15B-Final（收口）：**
- 完成 Phase 15 最终收口，更新 AGENTS.md 归档记录
- TypeScript 检查 Exit Code: 0

**Phase 16A-Audit + R1（ZenUML/Event Modeling 调研）：**
- 调研 ZenUML 和 Event Modeling 在 Mermaid 11.14.0 中的 runtime capability
- 确认两者均报 `UnknownDiagramError`，需外部 diagram 注册
- 更新 docs/MermaidRuntimeIsolation.md 标注为"暂不支持"
- 更新 AGENTS.md 记录决策

**Debug Log Cleanup Audit：**
- 检查 MermaidDiagram.tsx 和 LazyMermaidBlock.tsx
- 移除已识别的 `console.debug` 语句
- TypeScript 检查 Exit Code: 0

**Phase 17A-Audit → R1 → R1-V/V2 → Final（Markdown 列表主题适配）：**
- 审计确认列表正文/marker/checkbox/定义列表存在 P1/P2 问题
- 修改 `src/styles/markdown.css` 共 6 项 CSS 修复：
  - `li` 显式继承正文色（后回滚为自然继承）
  - `li::marker` 从 `var(--accent-cyan) + opacity: 0.5` 改为 `var(--text-secondary)`
  - 嵌套 marker 使用 `var(--text-muted)`
  - checkbox 增加 `border: 1px solid var(--border-subtle)` + `vertical-align: middle`
  - checkbox disabled 态增加 `opacity: 0.6`
  - 新增 `dl/dt/dd` 定义列表样式
- 多主题人工验证通过
- 无 P0/P1 问题，P2 进入 backlog
- TypeScript 检查 Exit Code: 0

**Phase 17B-R1 → Final（Markdown 强调文字主题适配）：**
- 回滚列表 `li` 的显式 `color: var(--text-primary)` — 改为自然继承
- 新增强调文字轻量主题化样式：
  - `strong/b`：仅 `font-weight: 700`，不变彩色
  - `em/i`：仅 `color: inherit`，不变彩色
  - `a`：移除 hover opacity，改用 `text-decoration-color` 过渡；新增 `a:visited` 固定主题色
  - `:not(pre) > code`：使用 `--inline-code-text/bg` 主题变量 + 轻量边框
  - `mark`：使用 `--search-highlight` 替代浏览器默认黄色
  - `kbd`：键帽感样式（hover-bg + border-subtle + box-shadow）
  - `samp`：类似 inline code 但无边框
  - `del`：`color: var(--text-muted)` 弱化
  - `ins`：`color: inherit` + 仅下划线使用 `var(--accent-green)`
- 确认设计原则：阅读优先，正文永远是视觉中心
- TypeScript 检查 Exit Code: 0

**MarkdownSyntaxExample.md YAML 头：**
- 添加 author: 宝藏二哥AIA
- 添加 description 描述文档作用

**Phase 18-Audit（TOC 初始点击定位审计）：**
- 审计了 TOC 点击链路、heading/TOC 映射、Mermaid 异步渲染、滚动恢复逻辑
- 确认根因：Mermaid 异步渲染完成后容器高度变化，导致 TOC 点击时 DOM 偏移量不准确
- 输出审计报告，建议采用"延迟 TOC 可点击 + 点击后二次校准"方案
- TypeScript 检查 Exit Code: 0

**Phase 18-R1（TOC 初始点击定位修复）：**
- 新建 `src/utils/scrollToHeading.ts`：`scheduleScrollCorrection()` 工具函数
- 修改 `src/components/reader/ReadingArea.tsx`：TOC 点击后 200ms/500ms/1100ms 多次校准
- 1500ms 布局稳定窗口（强校准 / 轻量校准模式）
- wheel/touchmove 取消后续校准
- 连续点击 TOC 取消旧校准任务
- TypeScript 检查 Exit Code: 0

**Phase 18-R2（阅读交互丝滑度小修）：**
- 四项修复：
  1. **TOC 校正柔化**：更新 `scrollToHeading.ts` 增加偏差分级（<=6px 不校、6~480px smooth、>=480px auto），调整校准延迟为 280/650/1200ms
  2. **图表编辑按钮位置**：`LazyMermaidBlock.tsx` 按钮容器增加 `gap-3 pr-2`，右移远离 TOC 区域
  3. **右键编辑段落修复**：新建 `readerStore.contextMenuEditTarget` + `setContextMenuEditTarget`；移除 `startQuickEdit` 的 `isQuickEditing` 守卫；App.tsx 右键菜单时存储编辑目标；MarkdownView 编辑目标改为从 readerStore 读取，移除旧 ref 模式
  4. **Wardley 地图主题适配**：MermaidDiagram.tsx 新增 wardley ChartColors 接口 + chartColors 赋值 + themeVariables（primaryColor/primaryTextColor/lineColor/primaryBorderColor）+ SVG 后处理（`.wardley-map` class 覆盖）
- TypeScript 检查 Exit Code: 0

### 进行中但未完成的任务

- **Phase 18-R2 Tauri 人工验证**：代码修改已完成，需用户在 Tauri 中验证全部四项修复

### 主动放弃或延后的任务

- ZenUML / Event Modeling 外部 diagram 注册：继续暂缓（从 Phase 16A 延后至今，不移入当前阶段）
- Mermaid ThemeVariables 中无效变量的进一步清理：已在 15B-R1 中完成合理范围的清理，后续无需继续

## 4. 当前开发计划

| 阶段 | 任务 | 状态 | 本地测试结果 | 备注 |
| ---- | ---- | ---- | ------------ | ---- |
| **Phase 15** | Mermaid 主题修复收口 | ✅ 全部通过 | TS: 0, Tauri 通过 |  |
| **Phase 16A** | ZenUML/Event Modeling 调研 | ✅ 完成 | 文档标注"暂不支持" |  |
| **Phase 17A** | Markdown 列表主题适配 | ✅ 全部通过 | TS: 0, Tauri 通过 | 无 P0/P1 |
| **Phase 17B** | Markdown 强调文字主题适配 | ✅ 全部通过 | TS: 0, Tauri 通过 | 阅读优先原则 |
| **Phase 18-Audit** | TOC 初始点击定位审计 | ✅ 完成 | — | 根因：Mermaid 异步渲染 |
| **Phase 18-R1** | TOC 初始点击定位修复 | ✅ 完成 | TS: 0 | scheduleScrollCorrection |
| **Phase 18-R2** | 阅读交互丝滑度小修 | ✅ 代码完成 | TS: 0 | **待 Tauri 验证** |
| — | Debug Log Cleanup | ✅ 完成 | TS: 0 | 提交前检查 |
| — | MarkdownSyntaxExample.md YAML | ✅ 完成 | — | author + description |

## 5. 本次会话新增的文件

| 文件 | 操作 | 目的 | 状态 | 备注 |
| ---- | ---- | ---- | ---- | ---- |
| `src/utils/scrollToHeading.ts` | 新建 | TOC 点击多次校准工具函数 | 已合入本地 | Phase 18-R1/18-R2 共用 |

## 6. 本次会话修改的文件

| 文件 | 修改摘要 | 风险级别 | 状态 | 备注 |
| ---- | -------- | -------- | ---- | ---- |
| `src/styles/markdown.css` | Phase 17A 列表 + Phase 17B 强调文字主题适配 | ⚠️ 中 | TS: 0, Tauri 已验证 | 作用域限定在 `.markdown-body` |
| `src/utils/scrollToHeading.ts` | Phase 18-R2 新增偏差分级校正逻辑 | 低 | TS: 0 | 新增功能，不改变现有行为 |
| `src/components/reader/ReadingArea.tsx` | Phase 18-R1 TOC 点击校正集成 + Phase 18-R2 延迟调整 | ⚠️ 中 | TS: 0 | import + refs + onScrollToBlock 逻辑 |
| `src/components/reader/LazyMermaidBlock.tsx` | Phase 18-R2 编辑按钮容器增加 `pr-2` | 低 | TS: 0 | CSS class 微调 |
| `src/stores/readerStore.ts` | Phase 18-R2 新增 `contextMenuEditTarget`；移除 `startQuickEdit` 守卫 | 低 | TS: 0 | 状态扩展 |
| `src/App.tsx` | Phase 18-R2 右键菜单时存储编辑目标到 store | 低 | TS: 0 | 新增 getState 调用 |
| `src/components/reader/MarkdownView.tsx` | Phase 18-R2 编辑目标改为 store 读取，移除 ref + onContextMenu | 低 | TS: 0 | 简化逻辑 |
| `src/components/reader/MermaidDiagram.tsx` | Phase 18-R2 新增 Wardley 地图主题适配 | ⚠️ 中 | TS: 0 | ChartColors + themeVariables + SVG 后处理 |
| `docs/MarkdownSyntaxExample.md` | 新增 YAML header（author + description） | 低 | — | 仅文档注释 |

## 7. 当前错误与未解决问题

| 项目 | 状态 |
| ---- | ---- |
| 本地构建状态 | `pnpm exec tsc --noEmit` Exit Code: 0 ✅ |
| 运行时状态 | Tauri 未运行（代码修改已完成，需人工验证 Phase 18-R2） |
| 类型检查 / Lint 错误 | 无 |
| 未解决的 bug | Phase 18-R2 四项修复 — 待 Tauri 验证；Wardley 主题适配效果取决于 Mermaid runtime 实际渲染 |
| 性能问题 | 无已知 |

## 8. 下一窗口的即时任务

1. **执行 Tauri 验证**：运行 `pnpm tauri dev`，在 MarkdownSyntaxExample.md 中验证：
   - TOC 校正柔化：首次打开长文档点击 6.28，校正过程比 R1 柔和，最终定位准确
   - 图表编辑按钮位置：右键编辑图表后，取消/保存按钮不会触发 Hover TOC
   - 右键编辑段落：右键菜单"编辑此段落"可正常进入编辑，无明显卡顿；双击编辑仍正常
   - Wardley 地图（6.27）：暗色主题下正常显示，不出现白底刺眼
2. **Phase 18 归档**：验证通过后更新 AGENTS.md，提交代码
3. **后续规划**：考虑进入 OKLCH 颜色工具、性能模式 UI、单元测试等 backlog 事项

**风险警告**：
- `scheduleScrollCorrection` 的偏差分级逻辑是本次新增行为，需确认不会导致校正过弱（定位不准确）
- Wardley 地图的 SVG 后处理 class 名（`.wardley-map`）基于 Mermaid 11.14.0 实际渲染，升级 Mermaid 版本后需复查
- `LazyMermaidBlock` 按钮位置调整仅加了间隙，如果 TOC hover 区域仍能触发，可能需要更大偏移
- `startQuickEdit` 移除了 `isQuickEditing` 守卫，需确认快速连续编辑场景不出现异常

## 9. 下一窗口的文件上下文清单

| 阶段 / 任务 | 需要 @ 或 pin 的文件 | 原因 |
| ----------- | -------------------- | ---- |
| 18-R2 Tauri 验证 | `src/utils/scrollToHeading.ts` | TOC 校正柔化逻辑 |
| 18-R2 Tauri 验证 | `src/components/reader/MermaidDiagram.tsx` | Wardley 主题适配 |
| 18-R2 Tauri 验证 | `src/components/reader/LazyMermaidBlock.tsx` | 图表编辑按钮位置 |
| 18-R2 Tauri 验证 | `src/components/reader/ReadingArea.tsx` | TOC 点击校正延迟 |
| 18-R2 Tauri 验证 | `docs/MarkdownSyntaxExample.md` | 测试文档（6.27 Wardley 等） |
| AGENTS.md 归档 | `AGENTS.md` | 更新 Phase 18 完成状态 |

## 10. 紧凑状态摘要

```md
## 紧凑摘要
- 当前阶段：Phase 18-R2（阅读交互丝滑度小修）— 代码完成，待 Tauri 验证
- 上次完成：Phase 18-R1（TOC 初始点击定位修复，TS: 0）
- 下一步建议：执行 `pnpm tauri dev`，验证四项修复（TOC 柔化/图表按钮/右键编辑/Wardley），通过后归档 Phase 18
- 阻塞点：无代码阻塞，需用户人工 Tauri 验证
- 高风险文件：ReadingArea.tsx（onScrollToBlock 校准逻辑）、MermaidDiagram.tsx（Wardley SVG 后处理）、scrollToHeading.ts（偏差分级新逻辑）
- 待用户验证项：TOC 校正柔和度、图表编辑按钮不触发 TOC、右键编辑段落可进入、Wardley 暗色主题可读
```
